# doc_qa 文档问答功能 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `doc_qa` intent to the agent so users can ask questions about pre-generated API documentation.

**Architecture:** New `doc_qa` node with its own `qa_tools` ToolNode forming an independent ReAct loop, parallel to `doc_gen`. Reuses existing `read_document` and `list_documents` tool functions. Intent recognition expanded to classify `doc_qa` vs `doc_gen`.

**Tech Stack:** LangGraph StateGraph, LangChain ChatOpenAI, existing `doc_storage` tools, Chainlit UI.

**Spec:** `docs/superpowers/specs/2026-03-12-doc-qa-design.md`

---

## Chunk 1: Nodes & Routing

### Task 1: `route_doc_qa` routing function

**Files:**
- Modify: `src/graph/nodes.py:116-121` (after `route_doc_gen`)
- Test: `tests/graph/test_nodes.py`

- [ ] **Step 1: Write failing tests for `route_doc_qa`**

Add a new test class at the end of `tests/graph/test_nodes.py`:

```python
class TestRouteDocQa:
    """doc_qa 路由函数测试。"""

    def test_routes_to_qa_tools_when_tool_calls_present(self):
        from src.graph.nodes import route_doc_qa

        ai_msg = AIMessage(
            content="",
            tool_calls=[{"name": "read_document", "args": {"module_name": "user", "api_name": "CreateUser"}, "id": "1"}],
        )
        state = {"messages": [ai_msg], "intent": "doc_qa", "confidence": 0.9, "params": {}}
        assert route_doc_qa(state) == "qa_tools"

    def test_routes_to_end_when_no_tool_calls(self):
        from src.graph.nodes import route_doc_qa
        from langgraph.graph import END

        ai_msg = AIMessage(content="根据文档，CreateUser 接口需要以下参数...")
        state = {"messages": [ai_msg], "intent": "doc_qa", "confidence": 0.9, "params": {}}
        assert route_doc_qa(state) == END
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/graph/test_nodes.py::TestRouteDocQa -v`
Expected: FAIL with `ImportError: cannot import name 'route_doc_qa'`

- [ ] **Step 3: Implement `route_doc_qa`**

Add after `route_doc_gen` in `src/graph/nodes.py` (after line 121):

```python
def route_doc_qa(state: State) -> str:
    """根据 LLM 是否发起工具调用决定下一步。"""
    last_message = state["messages"][-1]
    if hasattr(last_message, "tool_calls") and last_message.tool_calls:
        return "qa_tools"
    return END
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/graph/test_nodes.py::TestRouteDocQa -v`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add src/graph/nodes.py tests/graph/test_nodes.py
git commit -m "feat: add route_doc_qa routing function"
```

---

### Task 2: Expand `route_by_intent` to support `doc_qa`

**Files:**
- Modify: `src/graph/nodes.py:34` (`INTENT_LIST`) and `src/graph/nodes.py:109-113` (`route_by_intent`)
- Test: `tests/graph/test_nodes.py`

- [ ] **Step 1: Write failing test**

Add to the existing `TestRouteByIntent` class in `tests/graph/test_nodes.py`:

```python
    def test_routes_to_doc_qa_for_doc_qa_intent(self):
        from src.graph.nodes import route_by_intent

        state = {"intent": "doc_qa", "confidence": 0.9, "params": {}, "messages": []}
        assert route_by_intent(state) == "doc_qa"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/graph/test_nodes.py::TestRouteByIntent::test_routes_to_doc_qa_for_doc_qa_intent -v`
Expected: FAIL with `AssertionError: assert '__end__' == 'doc_qa'`

- [ ] **Step 3: Implement changes**

In `src/graph/nodes.py`, change line 34:

```python
INTENT_LIST = "doc_gen, doc_qa"
```

And update `route_by_intent` (lines 109-113):

```python
def route_by_intent(state: State) -> str:
    """根据意图识别结果路由到对应节点。"""
    if state["intent"] == "doc_gen":
        return "doc_gen"
    if state["intent"] == "doc_qa":
        return "doc_qa"
    return END
```

- [ ] **Step 4: Run tests to verify all pass**

Run: `uv run pytest tests/graph/test_nodes.py::TestRouteByIntent -v`
Expected: PASS (4 tests — the 3 existing + 1 new)

- [ ] **Step 5: Commit**

```bash
git add src/graph/nodes.py tests/graph/test_nodes.py
git commit -m "feat: expand route_by_intent to support doc_qa"
```

---

### Task 3: `doc_qa` node function

**Files:**
- Modify: `src/graph/nodes.py` (after `TOOLS` definition, around line 81)
- Test: `tests/graph/test_nodes.py`

- [ ] **Step 1: Write failing tests**

Add a new test class in `tests/graph/test_nodes.py`:

```python
class TestDocQa:
    """文档问答节点测试。"""

    @pytest.mark.asyncio
    @patch("src.graph.nodes.ChatOpenAI")
    async def test_returns_messages_with_ai_response(self, mock_chat_cls):
        from src.graph.nodes import doc_qa

        mock_llm = MagicMock()
        mock_llm_with_tools = MagicMock()
        mock_llm.bind_tools.return_value = mock_llm_with_tools

        ai_msg = AIMessage(content="CreateUser 接口需要 username 和 email 两个参数。")
        mock_llm_with_tools.ainvoke = AsyncMock(return_value=ai_msg)
        mock_chat_cls.return_value = mock_llm

        state = {
            "messages": [HumanMessage(content="CreateUser 的请求参数有哪些？")],
            "intent": "doc_qa",
            "confidence": 0.9,
            "params": {},
        }

        result = await doc_qa(state, RunnableConfig())

        assert "messages" in result
        assert result["messages"] == [ai_msg]

    @pytest.mark.asyncio
    @patch("src.graph.nodes.ChatOpenAI")
    async def test_binds_qa_tools_to_llm(self, mock_chat_cls):
        from src.graph.nodes import doc_qa

        mock_llm = MagicMock()
        mock_llm_with_tools = MagicMock()
        mock_llm.bind_tools.return_value = mock_llm_with_tools
        mock_llm_with_tools.ainvoke = AsyncMock(
            return_value=AIMessage(content="done")
        )
        mock_chat_cls.return_value = mock_llm

        state = {
            "messages": [HumanMessage(content="CreateUser 怎么用？")],
            "intent": "doc_qa",
            "confidence": 0.9,
            "params": {},
        }

        await doc_qa(state, RunnableConfig())

        mock_llm.bind_tools.assert_called_once()
        tools_arg = mock_llm.bind_tools.call_args[0][0]
        tool_names = [t.name for t in tools_arg]
        assert "read_document" in tool_names
        assert "list_documents" in tool_names
        assert len(tool_names) == 2

    @pytest.mark.asyncio
    @patch("src.graph.nodes.ChatOpenAI")
    async def test_prepends_system_prompt_to_messages(self, mock_chat_cls):
        from src.graph.nodes import doc_qa

        mock_llm = MagicMock()
        mock_llm_with_tools = MagicMock()
        mock_llm.bind_tools.return_value = mock_llm_with_tools
        mock_llm_with_tools.ainvoke = AsyncMock(
            return_value=AIMessage(content="done")
        )
        mock_chat_cls.return_value = mock_llm

        human_msg = HumanMessage(content="CreateUser 的参数有哪些？")
        state = {
            "messages": [human_msg],
            "intent": "doc_qa",
            "confidence": 0.9,
            "params": {},
        }

        await doc_qa(state, RunnableConfig())

        invoke_args = mock_llm_with_tools.ainvoke.call_args[0][0]
        assert invoke_args[0].type == "system"
        assert invoke_args[-1] == human_msg
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/graph/test_nodes.py::TestDocQa -v`
Expected: FAIL with `ImportError: cannot import name 'doc_qa'`

- [ ] **Step 3: Implement `QA_TOOLS` and `doc_qa` node**

In `src/graph/nodes.py`, add after the `TOOLS` list (after line 81):

```python
QA_TOOLS = [read_document, list_documents]


async def doc_qa(state: State, config: RunnableConfig) -> dict:
    """文档问答节点。

    使用 doc_qa 提示词和绑定工具的 LLM 回答文档相关问题。
    与 qa_tools ToolNode 形成 ReAct 循环。
    """
    prompt = load_prompt("doc_qa")
    user_input = state["messages"][0].content

    system_messages = prompt.format_messages(user_input=user_input)

    llm = ChatOpenAI(
        base_url=settings.llm.base_url,
        api_key=settings.llm.api_key,
        model=settings.llm.model,
    )
    llm_with_tools = llm.bind_tools(QA_TOOLS)

    all_messages = system_messages + state["messages"]
    response = await llm_with_tools.ainvoke(all_messages, config=config)

    logger.info("文档问答节点调用完成")
    return {"messages": [response]}
```

This step requires the `doc_qa` prompt files to exist. Create them now (they will be validated in Task 4, but the node needs them to not crash):

Create `src/prompts/system/doc_qa.md`:

```
你是一个基于已生成 API 文档的问答助手。

你的任务是根据用户的问题，查找并阅读相关的 API 文档，然后基于文档内容准确回答用户的问题。

## 工作流程

1. 分析用户问题，判断需要查找哪个模块或接口的文档
2. 如果不确定目标文档，先使用 `list_documents` 查看可用文档列表
3. 使用 `read_document` 读取目标文档内容
4. 基于文档内容准确回答用户问题

## 约束

- 只能基于文档内容回答，不得编造信息
- 如果文档中找不到答案，明确告知用户"文档中未找到相关信息"
- 回答使用中文
- 引用文档中的具体内容时，保持原文准确性
```

Create `src/prompts/user/doc_qa.md`:

```
用户问题：{user_input}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/graph/test_nodes.py::TestDocQa -v`
Expected: PASS (3 tests)

- [ ] **Step 5: Commit**

```bash
git add src/graph/nodes.py src/prompts/system/doc_qa.md src/prompts/user/doc_qa.md tests/graph/test_nodes.py
git commit -m "feat: add doc_qa node with QA_TOOLS and prompt templates"
```

---

## Chunk 2: Graph Wiring, Prompt & UI

### Task 4: Update intent recognition prompt

**Files:**
- Modify: `src/prompts/system/intent.md`

- [ ] **Step 1: Update intent prompt**

Replace the content of `src/prompts/system/intent.md` with:

```
你是一个智能代码文档生成助手的意图识别模块。

你的任务是分析用户的输入，判断其意图属于以下哪一类：

可用意图：
- doc_gen：用户要求为某个文件或模块生成 API 文档
- doc_qa：用户基于已有文档提问（查询接口参数、用法、错误码等）

请以 JSON 格式输出你的判断结果，包含以下字段：
- intent: 识别出的意图类别
- confidence: 置信度（0-1）
- params: 从用户输入中提取的关键参数
```

Note: The `{{intent_list}}` template variable is removed from this prompt. The intent list is now hardcoded directly in the prompt text for clarity. The `INTENT_LIST` constant in `nodes.py` still serves as the code-level reference (used by `route_by_intent`), but the prompt no longer uses it as a template variable.

- [ ] **Step 2: Update `intent_recognize` node to match**

In `src/graph/nodes.py`, update the `intent_recognize` function. Since the prompt no longer has `{intent_list}` as a template variable, change:

```python
    messages = prompt.format_messages(
        intent_list=INTENT_LIST,
        user_input=user_input,
    )
```

to:

```python
    messages = prompt.format_messages(
        user_input=user_input,
    )
```

- [ ] **Step 3: Run existing intent tests to verify nothing breaks**

Run: `uv run pytest tests/graph/test_nodes.py::TestIntentRecognize -v`
Expected: PASS (4 tests)

- [ ] **Step 4: Commit**

```bash
git add src/prompts/system/intent.md src/graph/nodes.py
git commit -m "feat: update intent prompt with doc_qa description"
```

---

### Task 5: Wire graph with `doc_qa` and `qa_tools` nodes

**Files:**
- Modify: `src/graph/graph.py`
- Test: `tests/graph/test_graph.py`

- [ ] **Step 1: Write failing tests**

Add to `TestBuildGraph` class in `tests/graph/test_graph.py`:

```python
    def test_graph_has_doc_qa_and_qa_tools_nodes(self):
        from src.graph.graph import build_graph

        graph = build_graph()
        node_names = set(graph.nodes.keys())
        assert "doc_qa" in node_names
        assert "qa_tools" in node_names
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/graph/test_graph.py::TestBuildGraph::test_graph_has_doc_qa_and_qa_tools_nodes -v`
Expected: FAIL with `AssertionError`

- [ ] **Step 3: Update `graph.py`**

Replace the full content of `src/graph/graph.py`:

```python
"""图编排。

构建并编译 LangGraph StateGraph，串联意图识别、文档生成、文档问答和工具执行。
"""

from __future__ import annotations

from langgraph.graph import START, StateGraph
from langgraph.prebuilt import ToolNode

from src.graph.nodes import (
    TOOLS,
    QA_TOOLS,
    State,
    doc_gen,
    doc_qa,
    intent_recognize,
    route_by_intent,
    route_doc_gen,
    route_doc_qa,
)


def build_graph() -> StateGraph:
    """构建并编译 agent 工作流图。

    Returns:
        编译后的 CompiledStateGraph，可直接 invoke。
    """
    graph = StateGraph(State)

    graph.add_node("intent_recognize", intent_recognize)
    graph.add_node("doc_gen", doc_gen)
    graph.add_node("tools", ToolNode(tools=TOOLS))
    graph.add_node("doc_qa", doc_qa)
    graph.add_node("qa_tools", ToolNode(tools=QA_TOOLS))

    graph.add_edge(START, "intent_recognize")
    graph.add_conditional_edges("intent_recognize", route_by_intent, ["doc_gen", "doc_qa", "__end__"])
    graph.add_conditional_edges("doc_gen", route_doc_gen, ["tools", "__end__"])
    graph.add_edge("tools", "doc_gen")
    graph.add_conditional_edges("doc_qa", route_doc_qa, ["qa_tools", "__end__"])
    graph.add_edge("qa_tools", "doc_qa")

    return graph.compile()
```

- [ ] **Step 4: Run all graph tests**

Run: `uv run pytest tests/graph/ -v`
Expected: PASS (all tests including the new one)

- [ ] **Step 5: Commit**

```bash
git add src/graph/graph.py tests/graph/test_graph.py
git commit -m "feat: wire doc_qa and qa_tools into the graph"
```

---

### Task 6: Update Chainlit UI

**Files:**
- Modify: `app.py:22` (welcome message), `app.py:50` (node filter), `app.py:58` (fallback message)
- Test: `tests/test_app.py`

- [ ] **Step 1: Write failing test for doc_qa streaming**

Add a new test to `TestOnMessageCoreFlow` in `tests/test_app.py`:

```python
    @pytest.mark.asyncio
    async def test_streams_doc_qa_node_content(self, app_module):
        """graph.astream yields AIMessageChunks from doc_qa; content
        must be forwarded via answer.stream_token()."""
        app, mock_cl, mock_graph = app_module

        chunks = [
            (AIMessageChunk(content="根据文档"), {"langgraph_node": "doc_qa"}),
            (AIMessageChunk(content="，CreateUser 需要 username 参数"), {"langgraph_node": "doc_qa"}),
        ]
        mock_graph.astream = MagicMock(return_value=_astream_from(chunks))

        user_msg = MagicMock()
        user_msg.content = "CreateUser 的参数有哪些？"

        await app.on_message(user_msg)

        answer_obj = mock_cl.Message.return_value
        calls = answer_obj.stream_token.call_args_list
        streamed = [c.args[0] for c in calls]
        assert "根据文档" in streamed
        assert "，CreateUser 需要 username 参数" in streamed
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_app.py::TestOnMessageCoreFlow::test_streams_doc_qa_node_content -v`
Expected: FAIL (doc_qa content not streamed because the filter only matches `"doc_gen"`)

- [ ] **Step 3: Update `app.py`**

Three changes in `app.py`:

(a) Welcome message (line 22):
```python
        content="你好！我是 doc-flow，你可以让我为 Go 源码文件生成 API 文档，或者基于已有文档提问。"
```

(b) Node filter (line 50):
```python
                and metadata["langgraph_node"] in ("doc_gen", "doc_qa")
```

(c) Fallback message (line 58):
```python
        answer.content = "抱歉，我目前支持文档生成和文档问答功能。你可以让我为某个文件生成文档，或者基于已有文档提问。"
```

- [ ] **Step 4: Update fallback test assertion**

In `tests/test_app.py`, update `test_fallback_message_when_no_doc_gen_output` (line 177):

```python
        assert "文档生成" in answer_obj.content
        assert "文档问答" in answer_obj.content
```

- [ ] **Step 5: Run all app tests**

Run: `uv run pytest tests/test_app.py -v`
Expected: PASS (all tests including the new one)

- [ ] **Step 6: Commit**

```bash
git add app.py tests/test_app.py
git commit -m "feat: update Chainlit UI to support doc_qa streaming"
```

---

### Task 7: Full test suite validation

- [ ] **Step 1: Run the complete test suite**

Run: `uv run pytest tests/ -v`
Expected: ALL PASS

- [ ] **Step 2: If any failures, fix and re-run**

- [ ] **Step 3: Final commit (if any fixes needed)**

```bash
git add -A
git commit -m "fix: resolve test failures from doc_qa integration"
```
